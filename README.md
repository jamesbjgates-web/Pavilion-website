# Pavilion Website V3

Refinement release:
- tighter header and vertical spacing
- anonymised proof/example module
- working form structure with PHP handler
- footer logo issue removed
- improved mobile navigation/layout
- SSL redirect template supplied separately

IMPORTANT:
1. In `contact-handler.php`, replace `REPLACE_WITH_PAVILION_EMAIL` with the real Pavilion recipient mailbox before relying on the form.
2. Do not deploy the HTTPS redirect until a valid SSL certificate is installed for both pspt.ltd and www.pspt.ltd.
3. Never commit passwords, API keys, mail passwords, customer data or private keys to this public repository.
